pipeline {
    agent {
        docker {
            image 'node:20-alpine'
            args '-v /tmp:/tmp' // Optional arguments
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
                sh 'npm --version'
            }
        }
    }
}
